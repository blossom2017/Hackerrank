#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    scanf("%d %d ",&size,&n);
    int *arr=malloc(size*sizeof(int));
    int i,j,k,l;
    for(i=0;i<size;i++)
        {
        scanf("%d",&arr[i]);
        
    }
    
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    return 0;
}

